function [a, b] = myDFT(Signal)
    M = length(Signal); %M



    %pre allocate space
    a = zeros(1, M);
    b = zeros(1, M);

    n = 0:(M-1);

    for k = n
     cos_f = ;
     sin_f = ;
 
     a(f+1) = 1/M * sum(Signal .* cos(2 * pi *k / M * n));
     b(f+1) = -1/M *(sum(Signal.* sin(2 * pi *k / M * n)));
    end
end

